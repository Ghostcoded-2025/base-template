# Copy to `.env` and fill in values from your Supabase project (Settings → API).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: canonical origin for Open Graph URLs during `vite-ssg build` (e.g. https://example.com)
VITE_SITE_URL=
